// Chakra imports
import { Box } from '@chakra-ui/react';

// Custom Chakra theme
const IPManage = (props: { [x: string]: any }) => {
  const { children, ...rest } = props;
  return (
    <Box>
      IPManage Page
    </Box>
  );
}

export default IPManage;